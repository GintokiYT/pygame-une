# Guia de instalación de Pygame

## 1. Ejecutamos primero este script en la raiz del proyecto
```bash
  pygame\Scripts\activate
```

## 2. Ejecutamos el siguiente comando
```bash
  pip install -r requirements.txt
```